[project]
name = "ghgql"
version = "0.1.0"
requires-python = "==3.13.*"
dependencies = [
    "gql[requests]>=4.0.0",
    "requests>=2.32.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
