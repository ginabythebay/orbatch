Read CLAUDE.md, CONVENTIONS.md, and CONTEXT.md first, then judge the
change against them: comment and docstring rules, type hints, import
placement, package structure, the ops/ dependency rule, and schema
changes shipping a migration. Also flag documentation the change makes
stale — stale docs are in scope. Report only violations you can cite a
line of guidance for.
