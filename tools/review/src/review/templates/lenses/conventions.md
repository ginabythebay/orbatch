Find this repository's guidance entry point — the agent instructions
file at its root — and follow the links in it to every other guidance
document it names, then judge the change against all of them: comment
and docstring rules, type hints, import placement, package structure,
the dependency rules between packages, and what a schema change has to
ship alongside it. Also flag documentation the change makes stale —
stale docs are in scope. Report only violations you can cite a line of
guidance for.
