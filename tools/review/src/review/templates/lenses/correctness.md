Hunt for defects: logic errors, wrong boundary conditions, unhandled
error paths, races, resource leaks, breaking changes to callers the
diff did not update, and data or migration steps that cannot be
rolled back. Trace the changed code's callers and callees rather than
reading hunks in isolation.
