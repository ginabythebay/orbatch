[project]
name = "snippets"
version = "0.1.0"
requires-python = "==3.13.*"
dependencies = [
    "click>=8.5",
    "ghgql",
    "orbit",
    "rich>=14.0",
]

[project.scripts]
snippets = "snippets.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
